'use strict';

describe('myApp.Calendar module', function() {

  beforeEach(module('myApp.Tasks'));

  describe('Calendar controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var CalCtrl = $controller('CalCtrl');
      expect(Caltrl).toBeDefined();
    }));

  });
});
